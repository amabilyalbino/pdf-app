{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main Ops PDF Studio window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "fs:write-files"
  ]
}
